apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  name: whereami
  namespace: ha
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name: http-port
  selector:
    app: whereami
  type: ClusterIP
status:
  loadBalancer: {}
